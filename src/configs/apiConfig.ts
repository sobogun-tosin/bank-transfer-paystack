const BASE_URL = "https://api.paystack.co";

export const bank_api = `${BASE_URL}/bank`;
export const recipient_api = `${BASE_URL}/transferrecipient`;
export const transfer_api = `${BASE_URL}/transfer`;
